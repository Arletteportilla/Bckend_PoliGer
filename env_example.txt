# Variables de Entorno para Backend PoliGer
# Copiar este archivo como .env en el directorio BACK/backend/

# Configuración de Django
DJANGO_SECRET_KEY=tu_clave_secreta_muy_segura_aqui
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Base de Datos
# Opción 1: SQLite (Desarrollo - por defecto)
DB_ENGINE=sqlite3
DB_NAME=db.sqlite3

# Opción 2: PostgreSQL (Producción - recomendado)
# Descomentar estas líneas y comentar las de SQLite para usar PostgreSQL:
# DB_ENGINE=postgresql
# DB_NAME=laboratorio_db
# DB_USER=laboratorio_user
# DB_PASSWORD=tu_password_seguro
# DB_HOST=localhost
# DB_PORT=5432

# Configuración de CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8081,http://127.0.0.1:8081

# Configuración de JWT
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# Configuración de Logging
LOG_LEVEL=INFO
LOG_FILE=logs/laboratorio.log

# Configuración de Cache
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache
CACHE_LOCATION=unique-snowflake

# Configuración de Modelos ML
ML_MODEL_CACHE_TIMEOUT=3600
ML_MODEL_PATH=data/models/

# Configuración de Reportes
REPORTS_PATH=data/reports/
EXCEL_TEMPLATES_PATH=data/templates/

# Configuración de Monitoreo
SENTRY_DSN=
ENABLE_SENTRY=False

# Configuración de Email (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Configuración de Redis (opcional)
REDIS_URL=redis://localhost:6379/0
USE_REDIS_CACHE=False

# Configuración de Producción
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True
