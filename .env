# Activa la configuración de PostgreSQL en settings.py
DB_ENGINE=postgresql

# Nombre de la base de datos que creaste en pgAdmin
DB_NAME=poliger_db

# Usuario de PostgreSQL (usaremos 'postgres' por ahora)
DB_USER=postgres

# Contraseña que definiste al instalar PostgreSQL
DB_PASSWORD=root

# Host y puerto (estándar)
DB_HOST=localhost
DB_PORT=5432

# ============================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# ============================================================================
# Cuenta Gmail que enviará los correos
EMAIL_HOST_USER=cartuchearlett@gmail.com
# App Password de Gmail (Google Account > Security > 2-Step Verification > App Passwords)
EMAIL_HOST_PASSWORD=fqulfcqahqrvzuoa

